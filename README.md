## The testreact project.

What's this project about? Loading public Meetup data using its
API, storing the data in Redux, representing it in React, and
plugging it into a Mezzanine page using Bootstrap.
